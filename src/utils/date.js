export const dateDiff = (start, end) => {
  console.log(start, end)
//   console.log(Date.parse(start), Date.parse(end))
//   if (typeof start !== 'undefined' && typeof end !== 'undefined') {
//     const time = (end.getTime() - start.getTime()) / 1000
//     const y = Math.abs(Math.round(time / (60 * 60 * 24) / 365.25))
//     const m = Math.abs(Math.round(time / (60 * 60 * 24 * 7 * 4)))
//     const d = Math.abs(Math.round(time / (3600 * 24)))

//     console.log(`${y} år ${m} månader ${d} dagar`)

//     return `${y} år ${m} månader`
//   }
}
