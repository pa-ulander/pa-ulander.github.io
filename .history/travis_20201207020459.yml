language: node_js
before_script:
  - npm install -g gatsby-cli
node_js:
  - "10"
branches:
  only:
  - main 
deploy:
  provider: script
  script: yarn install && yarn run deploy
  skip_cleanup: true
  github_token: $GH_TOKEN
  keep_history: true
  on:
    branch: main